name: "nikkei225_dbt"
version: "1.0.0"
config-version: 2

profile: "nikkei225"

model-paths: ["models"]
test-paths: ["tests"]

models:
  nikkei225_dbt:
    staging:
      +materialized: table
    intermediate:
      +materialized: table
    marts:
      +materialized: table